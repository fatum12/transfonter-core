fonts = FontsInFile($1)
n = SizeOf(fonts)
i = 0
while (i < n)
	Open($1 + "(" + fonts[i] + ")", 1)
	filename = ToString(i) + "-" + $fontname + ".ttf"
	Generate(filename)
	Print(filename)
	Close()
	++i
endloop