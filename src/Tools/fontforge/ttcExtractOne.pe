# $1 - path to TTC file
# $2 - font name
# $3 - index
# $4 - path to destination directory

Open($1 + "(" + $2 + ")", 1)

ext = ".ttf"
if ($order == 3)
    ext = ".otf"
endif

filename = $fontname + "-" + $3 + ext
Generate($4 + "/" + filename)
Print(filename)
Close()
